const routes = {
    home: '/home',

    login: '/login',
    register: '/register',
    forget: '/forget',

    vocabulary: '/vocabulary',
    createVocabulary: '/vocabulary/create'
} as const;

export default routes;
